# ----------------------------------------------------------------------------
# Runtime configuration for the AFFiNE remote MCP server.
# Set these as ENVIRONMENT VARIABLES in Coolify — do NOT commit real values.
# ----------------------------------------------------------------------------

# AFFiNE instance the MCP connects OUT to. Use AFFiNE Cloud or your self-hosted URL.
AFFINE_BASE_URL=https://app.affine.pro

# Access token generated in your AFFiNE account
# (Settings -> account -> API / access tokens). Starts with "ut_".
AFFINE_API_TOKEN=ut_xxxxxxxxxxxxxxxxxxxxxxxx

# Transport. Keep "http" for a remote server.
MCP_TRANSPORT=http

# Bind address. MUST be 0.0.0.0 in a container, otherwise the server listens on
# 127.0.0.1 only and the reverse proxy (Traefik/Coolify) can't reach it.
AFFINE_MCP_HTTP_HOST=0.0.0.0

# CORS allowed origins for browser clients (comma-separated).
# In oauth mode list the Claude web origins (ALLOW_ALL_ORIGINS is rejected).
AFFINE_MCP_HTTP_ALLOWED_ORIGINS=https://claude.ai,https://claude.com

# --- Auth between the CLIENT (Claude) and THIS MCP server ---
# oauth  : OAuth 2.1 — claude.ai web connector path. Validates JWT access tokens
#          issued by AFFINE_OAUTH_ISSUER_URL (JWKS). Requires PUBLIC_BASE_URL.
#          ALLOW_ALL_ORIGINS and HTTP_TOKEN are rejected in this mode.
# bearer : server requires  Authorization: Bearer <AFFINE_MCP_HTTP_TOKEN>
#          Works with Claude Code / Desktop. Claude WEB cannot send this header.
# none   : no transport auth — do not expose publicly without another protection.
AFFINE_MCP_AUTH_MODE=oauth

# Required when AFFINE_MCP_AUTH_MODE=oauth.
AFFINE_MCP_PUBLIC_BASE_URL=https://affine-mcp.karimou.me
AFFINE_OAUTH_ISSUER_URL=https://mcp-auth.karimou.me

# Only used when AFFINE_MCP_AUTH_MODE=bearer. Generate a long random secret.
#AFFINE_MCP_HTTP_TOKEN=change-me-to-a-long-random-secret
